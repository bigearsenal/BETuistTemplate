import SwiftUI

struct ContentView: View {
    var body: some View {
        Text(BigvalutIosStrings.Localizable.next)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
